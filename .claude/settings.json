{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && python3 \"$CLAUDE_PROJECT_DIR/.claude/scripts/humanizer.py\" \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Humanizing (stripping em dashes)"
          }
        ]
      }
    ]
  }
}
